import React from 'react';
import Menu from './menu';
import CharacterBuilder from '../../containers/character-builder';

const Layout = () => (
  <>
    <Menu />
    <CharacterBuilder />
  </>
);

export default Layout;
